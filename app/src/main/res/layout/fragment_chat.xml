<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   xmlns:app="http://schemas.android.com/apk/res-auto"
                                                   android:layout_width="match_parent"
                                                   android:layout_height="match_parent"
                                                   android:background="@color/white">

    <com.stfalcon.chatkit.messages.MessagesList
            android:id="@+id/messagesList"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/divider"
            app:incomingDefaultBubbleColor="@color/chatIncomingMessageBackground"
            app:incomingTextColor="@color/chatIncomingMessageText"
            app:outcomingDefaultBubbleColor="@color/chatOutgoingMessageBackground"
            app:outcomingTextColor="@color/chatOutgoingMessageText"/>

    <include layout="@layout/layout_divider"
             android:id="@+id/divider"
             android:layout_width="match_parent"
             android:layout_height="1dp"
             app:layout_constraintTop_toBottomOf="@id/messagesList"
             android:layout_marginTop="@dimen/chat_margin_small"
             android:layout_marginBottom="@dimen/chat_margin_small"
             app:layout_constraintBottom_toTopOf="@id/input"/>

    <com.stfalcon.chatkit.messages.MessageInput
            android:id="@+id/input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/appbar_height"
            app:layout_constraintBottom_toBottomOf="parent"
            app:inputHint="@string/chat_input_type_hint"
            app:showAttachmentButton="false"/>

</androidx.constraintlayout.widget.ConstraintLayout>