<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
                name="isVisible"
                type="Boolean"/>
        <import type="android.view.View"/>
    </data>

    <androidx.cardview.widget.CardView style="@style/CardViewLayout"
                                       android:visibility="@{(isVisible ?? true)? View.VISIBLE : View.GONE}">

        <fragment
            android:name="io.stanc.pogoradar.subscreen.RaidBossFragment"
            android:id="@+id/fragment_raidbosses"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </androidx.cardview.widget.CardView>
</layout>